{
  "name": "new_employe_test",
  "version": "1.0.0",
  "dependencies": {
    "electron": "latest",
    "mongoose": "latest",
    "@types/mongoose": "latest",
    "@types/mongodb": "latest",
    "shelljs": "latest"
  },
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . new_employe_test --ignore=.idea --overwrite --platform=win32 --arch=ia32 --asar=true --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.ProductName=\"Employe test App\""
  }
}
